export interface ModelUser {
    userId: string;
    storeName?: string;
    tel?: string;
    earnMileage?: number;
    mileageNumber?: number;
    ipAddress?: string;
    isPhone?: boolean;
    // ... 필요한 필드 추가
}